---
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout title="Opaline 数据与隐私" description="Opaline 本地优先数据与隐私说明。">
  <section class="page-hero">
    <div class="site-shell">
      <p class="eyebrow">Local first</p>
      <h1>数据优先留在你的工作区</h1>
      <p class="page-intro">
        Opaline 的耐久内容是本地 HTML 文档、图片和附件。应用状态、索引和缓存放在工作区元数据目录中。
      </p>
    </div>
  </section>

  <section class="section">
    <div class="site-shell content-grid">
      <article class="policy-row">
        <h2>典型工作区结构</h2>
        <pre><code>Opaline/
  notes/
  assets/
  .opaline/</code></pre>
        <p><code>notes</code> 保存 HTML 文档，<code>assets</code> 保存图片和附件，<code>.opaline</code> 保存元数据、设置、索引、缓存和历史快照。</p>
      </article>
      <article class="policy-row">
        <h2>可能发生的网络访问</h2>
        <ul class="fine-list">
          <li>用户配置 AI provider、测试模型或抓取模型列表。</li>
          <li>运行扩展组件或脚本。</li>
          <li>打开外部链接。</li>
          <li>手动检查应用更新。</li>
        </ul>
      </article>
      <article class="policy-row">
        <h2>API Key</h2>
        <p>用户应自行保管 AI provider 的 API Key。不要把个人 Key 写入公开仓库、截图或共享工作区。</p>
      </article>
      <article class="notice">
        <h2>Alpha 提醒</h2>
        <p>当前版本仍处于 public alpha。用重要数据测试之前，请先备份整个工作区，尤其是 <code>notes</code>、<code>assets</code> 和 <code>.opaline</code>。</p>
      </article>
    </div>
  </section>
</BaseLayout>
