---
import DocLayout from "../../../layouts/DocLayout.astro";
import { routeFor } from "../../../i18n";
---

<DocLayout
  lang="zh"
  page="docs/ai"
  title="搜索、AI 和发布"
  intro="Opaline 先做好可靠的本地搜索。语义检索、AI 辅助和发布能力都应当可解释、可授权。"
  eyebrow="文档"
  next={[
    { href: routeFor("zh", "privacy"), label: "隐私" },
    { href: routeFor("zh", "docs/release"), label: "构建与更新" },
  ]}
>
  <section>
    <h2>关键词搜索优先</h2>
    <p>关键词搜索是第一层可靠能力，因为它可以本地运行、容易解释，并且直接对应工作区里的 HTML 文档。</p>
    <p>语义搜索和 AI 检索是后续层。它们可以提升发现效率，但不能隐藏使用了哪些文档、标题或块。</p>
  </section>

  <section>
    <h2>可见来源</h2>
    <p>AI 回答应当标明依赖的工作区材料。当回答引用某篇文档时，用户应当可以打开来源并自行判断可信度。</p>
    <div class="doc-note">
      <p>私有文档不应在没有明确用户意图、provider 配置和可见范围的情况下发送到云模型。</p>
    </div>
  </section>

  <section>
    <h2>AI 编辑工具协议</h2>
    <p>AI 辅助编辑应当是命令，而不是静默重写。当前开发版已经把编辑器操作接入统一命令注册表，工具栏、右键菜单、Slash 命令、命令面板和未来 AI 编辑流程都调用同一层。</p>
    <ul>
      <li>命令暴露本地化名称、说明、分类、快捷键、参数 schema、可用性和 UI 可见性。</li>
      <li>AI 可见元数据标明权限、是否会修改笔记、是否需要确认、是否建议创建历史快照，以及风险等级。</li>
      <li>AI 操作计划可以先预览，检查未知命令、不可用命令、实验性命令和高风险命令。</li>
      <li>执行前会校验参数；执行失败时可以恢复到操作前的编辑器 HTML。</li>
    </ul>
    <p>这仍然只是安全编辑的基础设施。真正的 AI 改写体验还需要可读的变更预览、来源展示和用户确认流程。</p>
  </section>

  <section>
    <h2>发布</h2>
    <p>发布应当是从本地 HTML 文档出发的明确导出路径。它不应改变工作区真实数据源，也不应静默暴露私有材料。</p>
  </section>
</DocLayout>
