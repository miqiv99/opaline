---
import DocLayout from "../../../layouts/DocLayout.astro";

const updaterConfig = `{
  "plugins": {
    "updater": {
      "pubkey": "",
      "endpoints": []
    }
  }
}`;
---

<DocLayout lang="zh" page="docs/release" eyebrow="构建与更新" title="构建、发布和更新状态" intro="Opaline 当前处于 public alpha。发布构建主要用于测试和早期反馈。" next={[{ href: "/zh/download", label: "下载 alpha" }, { href: "/zh/docs/roadmap", label: "路线图" }]}>
  <section>
    <h2>当前 Release</h2>
    <p>当前版本是 <a href="https://github.com/miqiv99/opaline/releases/tag/v0.1.1" rel="noreferrer">GitHub 上的 v0.1.1</a>。官网不猜测具体 asset 文件名，Release 页面是下载来源。</p>
    <ul>
      <li>Windows 是当前主要开发和验证目标。</li>
      <li>macOS 安装包应在 macOS 上构建和测试。</li>
      <li>Linux 支持在明确测试和文档化前仍是后续方向。</li>
      <li>未签名安装包可能触发平台安全提醒。</li>
      <li>用重要笔记测试前，请先备份工作区。</li>
    </ul>
  </section>

  <section>
    <h2>当前开发版更新</h2>
    <p>v0.1.1 之后的源码已经继续推进编辑器基础设施，准备给后续 AI 生成笔记和更完整的编辑体验打底。</p>
    <ul>
      <li>统一编辑命令注册表：工具栏、右键菜单、Slash 命令、命令面板和未来 AI 流程共用同一层。</li>
      <li>新增 Slash 命令菜单和命令面板，覆盖常用格式、块和插入操作。</li>
      <li>AI 编辑工具协议增加命令描述、JSON schema 校验、权限、预览元数据、确认门槛、风险元数据、历史快照钩子和回滚钩子。</li>
    </ul>
  </section>

  <section>
    <h2>本地构建命令</h2>
    <pre><code>npm install
npm run tauri:dev
npm run tauri:build</code></pre>
    <p>Tauri 打包产物会写入 <code>src-tauri/target/release/bundle/</code>。</p>
  </section>

  <section>
    <h2>手动更新状态</h2>
    <p>设置页已经有“检查更新”流程。Opaline 只在用户点击按钮时检查更新，安装可用更新前还需要第二次明确确认。</p>
    <p>在 GitHub Release 资产、<code>latest.json</code> 和 Tauri updater 签名 key 配置完成前，更新配置应保持惰性。</p>
    <p><code>plugins.updater</code> 必须保留为对象，不能设成 <code>null</code>：</p>
    <pre><code>{updaterConfig}</code></pre>
  </section>

  <section>
    <h2>启用公开更新前</h2>
    <ul>
      <li>生成并在仓库外保护 Tauri updater 私钥。</li>
      <li>上传安装包、匹配的 <code>.sig</code>，以及指向真实 asset 的 <code>latest.json</code>。</li>
      <li>正式宣布更新前，先用非重要工作区测试安装流程。</li>
      <li>Public alpha 阶段不要启用自动后台安装。</li>
    </ul>
  </section>
</DocLayout>
