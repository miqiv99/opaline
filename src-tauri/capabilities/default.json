{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop permissions for the Opaline app shell.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
